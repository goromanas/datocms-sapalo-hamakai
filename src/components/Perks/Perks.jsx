import React from 'react'
import Section from '../Section/Section'

import { colors } from '../../config/colors'

const Perks = () => {
  return <Section color={colors.white}>Perks</Section>
}
export default Perks
